import {} from '../actions/pinnedActions.js'

const initialState = {
}

const pinnedReducer = (state = initialState, action) => {
  switch (action.type) {

    default: {
      return state
    }
  }
}

export default pinnedReducer
