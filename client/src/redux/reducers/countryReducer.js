import {} from '../actions/countryActions.js'

const initialState = {
}

const countryReducer = (state = initialState, action) => {
  switch (action.type) {

    default: {
      return state
    }
  }
}

export default countryReducer
